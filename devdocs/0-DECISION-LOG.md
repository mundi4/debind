# 결정 저널

**그날 있었던 논쟁과 결정**을 날짜별로.

`devdocs/`의 나머지 문서는 **결론**을 담는다. 이 파일은 **그 결론이 어떻게 움직였는지**를 담는다 —
누가 어느 방향을 잡았고, 누가 누구의 판단을 꺾었고, 무엇을 근거로 꺾었는지. 결론만 남으면 그
판단들이 통째로 안 보인다.

주제는 안 가른다. 주제별 문서 안에 묻히면 "무엇을 언제 누가 정했나"를 보려고 긴 문서 셋을
뒤져야 한다. 날짜 밑의 소제목은 그날의 갈래지 고정된 분류가 아니다.

파일 이름이 `0-`으로 시작하는 것은 목록에서 맨 위에 서기 위해서다.

## 무엇을 적나 — 셋뿐이다

**이 파일의 목적은 소유자의 판단을 돋보이게 하는 것이다.** 아래 셋은 그 목적을 거르는 체이지
목적 자체가 아니다. **셋 중 하나에 걸려도 그 목적에 안 보태면 안 적는다** — 구현하다 나온 자잘한
갈래, 표와 어긋난 자리 같은 것은 그 결정을 소유한 문서가 들면 된다.

다툼 없이 정해진 것도 **여기 안 적는다.** 코드 변경도 아니다 — git이 한다.

1. **부딪힌 자리** — 소유자와 내가 서로 다른 주장을 했을 때
2. **시각이 바뀐 자리** — 결정을 낸 근거가 다들 들여다보던 **틀 밖에서** 나왔을 때
   ("이건 UI를 근거로 판단해야 한다", "누가 이 포맷을 읽냐", 아무도 안 적어둔 의도)
3. **기존 결정을 완전히 뒤집을 때** — 좁히거나 고치는 것 말고. 무엇이었는지까지 적는다,
   그것이 살던 문서는 이미 넘어가 있을 테니

## 어떻게 적나

**1번과 3번은 양쪽을 다 쓴다.** 짝이 있는 것들이라 한쪽만 적으면 뜻이 반이다.

```markdown
**〈무엇을 놓고 갈렸나〉**

- **나** — 내 주장과 그것을 든 이유          ← 뒤집기면: **있던 결정** — 무엇이었고 왜 섰나
- **소유자** — *"그 사람 말 그대로"*

**누가 이겼고 왜.** (뒤집기면: 무엇이 되었고 왜)

`파일이름.md` — "절 제목"
```

내 쪽을 약하게 적으면 그건 허수아비고, **허수아비를 모아둔 기록은 값이 0이다.** 내가 왜 그렇게
생각했는지가 서 있어야 꺾은 쪽도 뜻이 생긴다. 뒤집기도 같다 — 옛 결정을 시시하게 적으면 뒤집은
것이 대단해 보이지 않고 그냥 정리로 읽힌다. 마찬가지로 **내가 진 자리를 깎지 않는다.**

**2번은 짝이 없으므로 인용으로 시작한다.** 겨루던 주장이 아니라 틀을 바꾼 한마디라서 그렇다.
인용이 곧 소유자 표시이고, 인용이 없으면 내가 낸 것이다.

## 나머지 규칙

- **근거는 여기 안 적는다.** 한 줄 요약과 어느 문서 어느 절인지만. 근거를 옮겨 적으면 원본과
  갈리고, 갈린 쪽이 거짓말을 해도 잡아줄 검사가 없다.
- **파일은 이름만 적는다. 경로도 줄 번호도 안 적는다.** 작업 문서는 다 만들면
  `devdocs/legacy/`로 가서 경로가 바뀌고, 줄 번호는 다음 편집에 낡는다. 이건 불완전한
  링크가 아니라 **의도한 것**이니 채워 넣지 말 것.
- **결정을 적는 같은 커밋에 이 줄도 들어간다.** 나중에 몰아서 쓰면 그 순간 두 번째 진실이
  되고, 이 저장소가 제일 싫어하는 모양이 된다.
- **없는 반전을 적지 않는다.** 내가 낸 안에 소유자가 동의한 것은 뒤집기가 아니다. 부풀리면
  진짜 반전까지 같이 의심받는다.
- **2026-08-15에 시작했다.** 그 전 결정은 각 문서의 절에 그대로 있고, 여기로 소급하지 않았다 —
  문서에서 재구성하는 것은 기록이 아니라 추정이다.

---

## 2026-08-15

> 전송 포맷의 근거 여섯이 무너지고, 그 포맷이 왜 저장 포맷을 닮았는지(그리는 코드 재사용)가
> 처음으로 문서에 적혔다. 애드온 경계가 "UI 축이 이긴다"로 정해졌다. `seq`가 밴드를 넘을 때
> 들쭉날쭉하다는 기존 결함이 드러나 별도 문서로 섰다.
>
> **이날 뒤집힌 것 대부분이 소유자 쪽이다.** 내가 스스로 잡은 것은 뮤테이션 테스트로 내 주석의
> 거짓을 찾은 것 하나뿐이고, 나머지 조사는 전부 소유자의 물음에서 출발했다.

### 전송 포맷

**전송 포맷의 최상위가 그룹이어야 하나**

- **나** — 여섯을 댔다. 키가 레이어를 가로지른다 · 키를 빼면 묶음이 사라진다 · 레이어는 액션마다
  다른 값이다 · 빨간 테스트가 그렇게 뒤집었다 · 순서가 그룹 안 1..n이어야 한다 · 읽는 사람이
  손대는 단위가 그룹이다.
- **소유자** — *"이게 왜 안 남아? key에 임의 문자열을 만들어서 넣으면 되는데"* ·
  *"누가 이 포맷을 읽냐?"*

**소유자. 여섯 다 무너졌다.** 묶음은 액션의 필드 하나로 실리므로 최상위와 무관하고, 압축된
바이트를 사람이 읽을 일이 없다. 나머지 넷도 같은 방식으로 넘어갔다 — 나는 "묶음이 명시적으로
실려야 한다"와 "그룹이 최상위여야 한다"를 한 덩어리로 취급하고 있었다.

`building-export-import.md` — "전송 포맷의 근거가 무너졌다"

---

**"내가 내보내기 포맷을 저장 포맷과 거의 일치하게 만든 이유는 재사용이야."**

가져오기 창이 프로필을 그리는 UI로 페이로드를 그릴 수 있게 하려던 것. **이 의도가 문서에 한 번도
안 적혀 있지 않았고**, 그래서 결과만 읽은 내가 근거 여섯을 지어냈다. 그리고 이 의도로 보면 지금
모양이 그 목적에서 **제일 먼** 모양이다 — 저장과 갈라지는 자리가 정확히 최상위 하나다.

`building-export-import.md` — "왜 저장 포맷을 닮았나"

---

**묶음을 무엇이 나르나**

- **나** — 액션마다 그룹 번호를 단다. 최상위가 무엇이든 그 필드가 묶음을 지킨다.
- **소유자** — *"그룹 11이라고 선언하는 것 자체가 없다 이거야. key가 같으면 같은 그룹이야."*

**소유자.** 식별자라는 개념 자체가 안 생긴다. 프로필이 이미 키로 묶어 그리고 있으므로 `group.id`도
`importGroup`도 같이 사라진다.

`building-export-import.md` — "key가 그룹을 나른다"

---

**"레이어 자체의 모양은 달라져야 할 게 별로 없는데?"**

선의 레이어 서술이 이미 저장 주소 그대로다. 번역이 필요했던 것은 둘뿐이었다 — 뷰 인덱스인 레이어
번호와, character에서 guid를 떼는 것. 나는 그것을 "번역"이라 부르며 근거로 세우고 있었다.

`building-export-import.md` — "레이어는 이미 저장 주소 그대로다"

---

**합성 키를 프로필이 감당할 수 있나**

- **나** — 못 한다. 가짜 키가 프로필로 새면 `SetBindingClick`이 죽은 키를 만든다.
- **소유자** — *"이미 bring이라는 중간 과정이 있는데 가짜 키 핸들링 못한다는 건 말도 안 되고."*

**소유자.** 임포트는 이미 번역 계층이다 — `setstate` 이름을 인덱스로, macro 스냅샷을
MACRO/MACROTEXT로, 주소를 `(scope, class, spec)`로 이미 바꾸고 있다. 같은 자리에서 `order`라는
**이름**도 같이 무너졌다: 그 이름을 쓴 근거였던 충돌은 애초에 일어날 수 없었다.

`building-export-import.md` — "seq를 그대로 싣고"

---

**"내보내기는 현재 상태의 스냅샷인데 왜 move 걱정을 해야 하냐."**

받는 쪽에서 일어나는 일을 내가 **선에 필드가 필요한 이유**로 끌어왔다. 옮겨놓고 보니 순환논증이라
받는 쪽에서도 안 남았다 — 필드는 어차피 실려 오니까.

`building-export-import.md` — "seq를 그대로 싣고"

---

**합성 키를 무엇으로 할 것인가**

- **나** — 문자열 접두사 `export#1`. 실제 키 코드에 `#`이 안 나온다는 것을 클라이언트 `KEY_*`
  표까지 뒤져 확인했다.
- **소유자** — *"아니면 타입비교로... key = 1,2,3 (number)"*

**소유자.** 접두사는 *"안 겹칠 것 같다"*이고 타입은 **겹칠 수 없다.** 그리고 빠뜨린 가드가 조용히
통과하는 대신 던진다 — 이 애드온이 제일 무서워하는 것이 조용히 죽는 키다.

`building-export-import.md` — "키를 빼고 보낼 때"

---

**매직넘버를 어디서 더하나**

- **나** — 내보낼 때 굽는다. 두 메시지 전 소유자 제안이었고, "임포트가 한 줄도 안 쓴다"를 근거로
  내가 동의했다.
- **소유자** — *"내보내기 할 때 매직넘버를 더해서 구울 필요가 없다."* 자기 제안을 자기가 되물렀다.

**소유자.** 상수를 알아야 하는 쪽은 정렬하는 쪽이고 그건 bring이다. 그리고 내가 든 "한 줄도 안
쓴다"는 틀렸다 — bring은 어차피 닿은 그룹을 재부여해야 한다.

`building-export-import.md` — "seq를 그대로 싣고"

---

**"`importGroup` · `importOrder` · `NextImportGroupID` · `CollectImportGroupActions`를 걷어낸다."**

문서의 "걷어내지는 것 목록"은 그 함수 하나만 *"남되 키 문자열용으로"*라고 적어두고 있었다. 그 줄은
합성 키를 숫자로 정하기 전에 쓴 것이라 이름부터 어긋나 있었고, 소유자가 구현 지시에서 통째로
잘랐다. 남은 것은 "저장 전체의 최댓값 + 1"이라는 기계 하나뿐이라 `NextSyntheticKey`로 축을 바꿔
다시 섰다 — 이름도 읽는 필드도 바뀌었으니 **남긴 것이 아니다.**

`building-export-import.md` — "걷어내지는 것 목록"

### `seq`와 밴드

**"지금 seq는 밴드 내에서 부여된 번호가 아니라 그룹 전체에서 부여된 번호다. PlaceLast가 같은
밴드인지 체크하는 로직을 갖고 있지 않아. 맞지?"** — 그리고 네 단계짜리 시나리오.

임포트 이야기를 하다 **임포트와 무관한 기존 결함**을 끌어냈다. 발급 범위(레이어 전체)와 의미
범위((레이어, 키))가 어긋나 있고, 밴드를 넘는 액션은 그 어긋남을 그대로 들고 간다. 내가 며칠째
그 주변 코드를 읽고 있었는데 못 본 자리다.

`renumbering-a-key-group.md` — "무엇이 잘못됐나"

---

**그 결함을 뭐라고 부를 것인가**

- **나** — "새로 온 것이 원래 있던 것을 앞지른다." 증상 하나를 집어 설명하고 있었다.
- **소유자** — *"저 behavior 자체가 문제라는 게 아니라 저 behavior는 결과가 들쭉날쭉이라는 게
  문제야. 어떨 때는 밴드의 맨 뒤, 어떨 때는 맨 앞, 어떨 때는 중간으로 들어간다."*

**소유자.** 앞/중간/뒤 중 어디로 갈지 정하는 것이 화면에 하나도 없다 — **틀린 규칙이 아니라 규칙의
부재**이고, 그건 배울 수도 예측할 수도 없어서 훨씬 나쁘다. 내 쪽 진단으로는 "그럼 앞지르지 않게
하자"로 갔을 텐데 그건 세 갈래 중 하나만 막는 것이다.

`renumbering-a-key-group.md` — "무엇이 잘못됐나"

---

**"이건 UI를 근거로 판단해야 한다. 지금 UI는 [^] [v] 버튼이다. 그렇기 때문에 가까운 곳으로
이동되는 게 맞다."**

(가)/(나)를 가른 논거. 나도 (나)로 기울어 있었지만 근거가 *"덜 움직이니까"* 정도였고, 규칙의
일관성만 저울에 올리고 있었다. **화면이 이미 답을 갖고 있다는 것**을 못 봤다 — 한 칸짜리 도구를
준 창이 열 칸짜리 이동을 제멋대로 하면 도구와 시스템의 단위가 안 맞는다.

`renumbering-a-key-group.md` — "왜 (나)인가"

---

**복구 불가가 (가)/(나)를 가르는 기준인가**

- **나** — (나)의 흠으로 세웠다. 조건을 껐다 켜면 제자리로 안 돌아온다.
- **소유자** — *"이건 재부여 하는 이상 불가능."*

**소유자.** 예측 가능성과 복구 가능성이 **구조적으로 양립 불가**다 — 착지를 보이는 것만으로
정하겠다는 것이 예측 가능성의 정의이고, 그러면 안 보이는 정보는 버려진다. 둘 다 잃으므로 두 안을
가르는 기준이 못 되고, 내가 흠으로 적어둔 것이 저울에서 빠졌다.

`renumbering-a-key-group.md` — "복구 불가는 구조적이다"

---

**`PlaceLast`를 제약으로 놓을 것인가** — 방법론

- **나** — 세 번에 걸쳐 *"`PlaceLast`가 `seq`를 지우니 값을 치러야 한다"*를 설계 비용으로 셌다.
- **소유자** — *"너의 placelast 함수 동작을 유지하려는 자세 자체가 나를 불안하게 만든다."*

**소유자.** 그 함수는 우리가 정한 설계에서 **할 일이 없는 함수**였다. 결정은 이미 났는데 코드를
고정점으로 잡고 그 주위를 설계하고 있었고, 그러느라 두 안을 비교하며 든 유일한 실질 비용이 통째로
무효였다.

`renumbering-a-key-group.md` — "손댈 자리"

---

**번호가 매직넘버를 넘을 수 있나**

- **나** — 상한이 없으니 마법 숫자는 근거가 없다. 압축이나 가드를 검토하고 있었다.
- **소유자** — *"한 키에 액션 10000를 넣어서 애드온이 제대로 동작하지 않는다고 누가 버그
  리포트를 하면... 나는 'wtf did u expect?'라고 답하겠다."*

**소유자.** 물음이 닫혔다. 가드도 assert도 "혹시 넘으면" 갈래도 안 넣는다 — 못 터지는 코드에
터지는 항목을 다는 것이 이 저장소가 싫어하는 것이고, 내가 그걸 하려던 참이었다.

`renumbering-a-key-group.md` — "크기 걱정은 안 한다"

---

**뒤집힌 것: `MoveKeyGroupToKey`의 "점유자를 먼저"**

- **있던 것** — 내가 그 함수에 *"점유자를 먼저 처리한다. 교체에서 그것들이 아직 옛 키에 있을 때
  번호를 매겨야 그 뒤에 선다"*고 이유까지 달아놨다.
- **뮤테이션** — 순서를 뒤집었는데 테스트가 안 빨개졌다.

**근거 없이 쓴 장담이었다.** 어느 답도 두 키 그룹을 한 키에 남기지 않고 `seq`는 한 키 안에서만
비교되므로 순서가 무관하다. 지우고 왜 무관한지로 바꿨다. — **이날 소유자를 거치지 않고 잡은 유일한
것.**

`Profile.lua` — `MoveKeyGroupToKey` 주석

---

**키를 떼도 `seq`를 남길 것인가** — 만들고 나서 다시 열렸다

- **나** — 남긴다. 문서도 그 전제로 서 있었고(뗐다 다시 걸면 제자리), 나는 그 위에 **뗄 때는
  재부여를 안 건다**는 예외를 하나 더 만들고 근거까지 붙였다(들고 나간 번호가 남은 그룹의 것과
  겹친다). 다른 키에 걸면 목적지 그룹 한가운데에 떨어지는 것을 재서 찾아놓고도, 그것을 남은 값의
  결함으로 안 읽고 **그 값을 지키는 쪽으로 설계를 비틀었다.** 소유자에게 물은 것도 "이게 맞는
  동작이냐"가 아니라 "고칠까요"였다.
- **소유자** — *"뗏다 걸었는지 새로 편입된 것인지를 감지할 수 없다면 seq는 틀린 값이고 seq의 값을
  사용하는 것도 틀린 행동이란거야."* · *"그리고 코드가 뗐다 붙인걸 감지할 수 있더라도 사용자가
  그걸 의식하지 못하면(오랜 시간 경과 후) 그것 역시 기존 seq에 의미를 부여하는게 틀린거고."*

**소유자.** 감지가 없다는 것은 그 값이 "여기에서의 자리"가 아니라 **어딘가에서의 번호**라는 뜻이고,
두 번째 문장이 우회로까지 닫았다 — 어느 키의 번호인지 같이 저장하면 감지는 되지만, 며칠 뒤 사용자는
그 액션이 그 키에 있었다는 것을 모른다. **키가 없는 액션은 `seq`를 갖지 않는다**로 갔다. 그러자 내
예외의 이유(겹침)가 사라져서 예외 자체가 없어졌고, 배치하는 자리 넷이 전부 "도착하면 맨 뒤"로
같아졌다.

`renumbering-a-key-group.md` — 머리말

### 애드온 경계

**분리를 유지할 값이 있나**

- **나** — 미리보기를 먼저 정하면 폴더가 따라 나온다. 재사용이면 옮기고, 자기 UI를 키우면 남긴다.
  값을 재서 긋자고 했다.
- **소유자** — *"UI는 포기할 수 없는 축이고 분리는 어느 정도까지 리소스 절약이고 그 정도는
  우리가 감내할 일이다."*

**소유자.** 최적화가 양보할 수 없는 축을 제약하게 두지 않는다. 그래서 이건 **재본 뒤에 바뀌는
종류의 결정이 아니고**, 내가 제안한 측정은 결정 입력이 아니라 참고로 내려갔다.

`building-export-import.md` — "애드온 경계"

---

**순서가 무엇인가**

- **나** — 미리보기 결정이 먼저고 폴더는 거기서 따라 나온다.
- **소유자** — *"순서가 틀렸다. UI는 debind에 모두 둬서 '우리가 원하는 UI'를 만든 후 분리할
  만한지를 재어봐야지. 분리한 후에 그 '분리'를 근거로 UI를 짜게 되면 실패한다."*

**소유자.** 내 순서대로면 **아직 안 한 설계의 입력으로 경계가 쓰인다.** 작업대가 이미 그렇게
죽었다 — 저쪽 폴더에 있으니 오버뷰에 손을 안 뻗고 자기 기능을 키웠고, 역전이 나서 접었다.
**원칙으로 승격했다.**

`building-export-import.md` — "애드온 경계"

---

**"오버뷰의 함수들이 가져오기 데이터에 의존하게 된 점. 로캘 문자열들이 기본 애드온에 있는 점."**

경계가 모호하다는 진단의 근거로 든 둘. 재보니 **문자열이 잘못된 자리에 있는 게 아니라 그것을 쓰는
화면이 다른 폴더에 있는 것**이었다 — 선/서랍 코드는 로캘을 0개 쓰고 패널 둘이 46개를 다 쓴다.
증상 둘이 같은 하나를 가리키고 있었다.

`building-export-import.md` — "선이 왜 하필 거기인가"

### 키 그룹 UI (화면으로 보고 꺾은 것)

**충돌 대화상자의 답이 몇 개인가**

- **나** — 셋 또는 넷. 교체는 자기 키가 있는 키 그룹만 할 수 있어서 그 경우에만 버튼을 세웠다
  (`DisplayButton2`).
- **소유자** — *"3은 합치기와 덮어쓰기 취소 3개면 된다."*

**소유자.** 답이 화면마다 둘이었다 셋이었다 하는 것보다 언제나 같은 셋이 낫다. 교체는 데이터
층에서도 지웠고, 되살릴 근거를 적어뒀다 — 두 키 그룹이 대등한 조작이라 이 대화상자의 물음에 안 맞으므로,
필요해지면 넷째 버튼이 아니라 따로 선 조작이어야 한다.

`building-export-import.md` — "교체를 뺐다"

---

**오버레이를 키 그룹 지정에 다시 쓸 수 있나**

- **나** — 같은 프레임에 글자만 갈아 끼웠다. 배선을 그대로 쓰고 안내 문장만 바꾸면 된다고 봤다.
- **소유자** — *"이건 비슷하게 생긴 오버레이가 두 가지 다른 방법으로 작동하는 건 문제임."*

**소유자.** 같은 옷을 입고 ESC의 뜻이 반대다. 문구를 더 정확히 쓰는 것으로는 안 풀린다 — 읽기 전에
이미 안다고 생각하기 때문이다. 안 셋을 적어두고 **아직 안 골랐다.**

`building-export-import.md` — "오버레이 둘이 똑같이 생겼다"

### 문서 구조

**로드맵 문서를 하나 더 질 값이 있나**

- **나** — 있다. 단 근거는 "버전↔트랙이 어디에도 없다"가 아니다. 그건 세 줄이고 일 년에 두 번
  바뀐다. 진짜 근거는 **그 일을 지금 `memory/project_status.md`가 하고 있다는 것** — 저장소
  밖이라 diff에 안 걸리고, 코드와 같이 안 움직이고, 낡아도 아무도 모른다.
- **소유자** — *"문서 하나 더 관리할 가치가 있는지"*

**나.** 새 파일을 지는 값이 아니라 **낡는 자리를 리뷰에 걸리는 자리로 내리는** 값이다. 조건을
달았다: 배정 표 밖으로 자라면 안 된다. 각 트랙이 어디까지 왔는지는 그 문서의 머리말이 답하고,
열린 일 목록은 `devdocs/` 최상단 그 자체다.

`0-ROADMAP.md` — 머리말

---

**내보내기 버튼의 낱말을 고르다가 클립보드로 갔다**

- **나** — 버튼 문구를 클라이언트 어휘에서 찾고 있었다. 특성·편집 모드·청사진을 뒤져 "이 물건에
  이름이 붙는 자리는 복사 확인 메시지 하나뿐"이라는 데까지 갔다.
- **소유자** — *"블리자드에서는 문자열을 바로 클립보드에 집어넣는다"* ·
  *"이게 우리는 허용이 안되어서 문자열 창을 띄우는지?"*

**소유자.** 물음이 "무슨 낱말이냐"에서 **"왜 문자열을 화면에 보여주느냐"**로 바뀌었다. 프로브로
재보니 `CopyToClipboard`는 애드온 코드에서 `ADDON_ACTION_FORBIDDEN`이고, 복사 창은 우리가 고른
모양이 아니라 **유일한 문**이었다 — 그때까지 그건 추측이었고 어디에도 안 적혀 있었다. 블리자드가
그 물건에 이름을 잘 안 붙이는 이유도 같이 풀렸다. 걔들은 화면에 안 보여준다.

`building-export-import.md` — 공유 창

---

**남는 애드온의 이름**

- **나** — `DebindCodes`. 이어서 `DebindVault`.
- **소유자** — *"볼트는 와우에서 사람을 희망고문하는 단어인데"* ·
  *"Debind Migration이란 애드온을 쓰고 있지. 같은 톤으로 생각해봐"*

**소유자.** 앞엣것은 기각 사유이고, 뒤엣것이 틀을 바꿨다. 그때까지 **둘 다 은유 목록 위에서**
고르고 있었다 — 내가 금고를 냈고 소유자가 Keep·Silo·Archive를 냈는데 셋 다 장소 은유다. 그
목록을 통째로 버리게 한 것이 저 한 줄이다: 우리 동반 애드온의 `## Title`은 은유가 아니라 **일을
그대로 말하는 평범한 명사**다(Debind Migration, Debind Clique Fake). 그 톤에서 답은 `Storage`이고,
클라이언트가 마침 그 뜻으로 쓰고 있다(하우징 **창고** — 소유하고 있지만 지금 배치돼 있지 않은 것).

`building-export-import.md` — "폴더 이름"

---

## 2026-08-16

**가져온 것이 내 키 그룹에 섞이는 것을 고치려다 그룹을 1급 모델로 세우는 데까지 갔고, 거기서 죽었다**

- **나** — 키 그룹을 1급으로 세우자. 그룹 = 키 스칼라, 계정 전역, 그룹 자체에 조작(묶음째 해제,
  묶음째 재키잉)이 붙는다. 근거는 "단축키를 해제하면서 그룹을 유지할 방법이 없다"였고, 그건
  소유자가 짚은 진짜 결함이었다.
- **소유자** — *"마법사에 r를 신비한 폭발을 걸어놨는데 그걸 t로 바꾼다고 치자. 그럼 드루이드가
  r로 사용하던 바인딩도 t로 바뀐다. 이건 사용자가 의도하지 않았을 확률이 높아."*

**소유자.** 예 하나가 모델을 죽였다. 키를 정체성으로 삼으면 **아무것도 공유하지 않는 것들이**
— 다른 직업, 다른 레이어, 같이 로그인해 있을 수도 없는 캐릭터 — 키 문자열 하나로 한 그룹이
된다. 내가 앞서 "그룹이 레이어를 넘으면 안 된다"고 했다가 소유자에게 꺾인 적이 있는데(그때 내가
댄 근거는 순서였고 그건 틀렸다), 결론은 다른 근거로 다시 섰다: **레이어가 정하는 것은 폭발
반경**이고, 반경을 걸친 묶음에 조작을 붙이면 한 번의 누름이 가장 넓은 멤버만큼 닿는다.

그 전에 소유자가 판정 기준부터 바꿔놨다 — *"그룹 자체에서 드랍다운 메뉴를 띄워야 한다. 그게
그룹이 1급인지 아닌지를 가른다."* 나는 "싼 것부터 만들어 보고 판단하자"며 액션 우클릭 메뉴에
넣으려 했는데, 그렇게 만들면 **실험이 언제나 "굳이 그룹일 필요 없네"로 끝난다.** 측정을
망가뜨리는 쪽을 싸다는 이유로 고르고 있었다.

되짚어 보니 애초에 키 그룹은 누가 짠 세트가 아니다 — 몇 달에 걸쳐 서로 다른 레이어에서 하나씩
쌓인 것이고, 그것을 통째로 조작하려 할 때마다 새 개념(반경 고지, 유도된 스코프)이 붙었다.
소유자가 거기서 멈췄다: *"이것도 너무 복잡하고... 의도치 않는 문제를 만들 수도 있다."*

`building-export-import.md` — "그룹을 1급으로 세우려던 것"

---

**"임시로 키를 끄기"가 그 아래 깔린 진짜 필요였고, 한계로 두기로 했다**

- **소유자** — *"임시로 키를 비활성화하기가 힘들잖아."*

**소유자.** 그룹을 파킹하려던 것이 실은 이걸 우회해서 하려던 것이었다 — 나는 액션을 **옮겨서**
풀려고 했는데, 옮기기는 파괴적이고 되돌리기 애매한 반면 끄기는 토글이다. 격리(`imported`)가
이미 "프로필에 있고 그려지고 어떤 키에도 안 닿는" 것을 하고 있으므로 패턴도 증명돼 있었다.

그런데 반경 문제가 여기서도 나왔다 — 계정 레이어 액션을 끄면 모든 캐릭터에서 꺼진다. 내가 그걸
피하려고 "캐릭터별 꺼둔 키 목록"을 냈고, 소유자가 잘랐다: *"바인딩이 살고 있는 위치는
레이어인데 이걸 캐릭터로 어떻게 강제하냐."* 레이어 위에 두 번째 스코프를 얹는 것이었다.
소유자가 그 자리에서 닫았다 — *"그냥 어쩔 수 없는 한계로 두고."*

`building-export-import.md` — "그룹을 1급으로 세우려던 것"

---

**내보내기의 "단축키는 빼고"를 없앴다**

- **소유자** — *"이런걸 공유할 사람이라면 자신의 단축키 시스템에 대해서 자부심을 느끼는
  사람이다. 그거 뺄 이유 없다. 개인정보도 아니고."*

**소유자.** 서 있던 결정이 통째로 뒤집혔다. 그 옵션은 "받는 사람이 자기 키를 고르게 한다"는
것이었는데, 받는 쪽이 **언제나** 키를 다시 매기게 되면서 남은 효과가 "내가 뭘 썼는지 안
알린다" 하나뿐이 됐다. 나는 그 지점까지만 짚고 "문구를 고칠까요"로 갔는데, 소유자는 **왜 그걸
감추려 하는가**를 물었다. 문자열에 실리는 것 중 감출 이유가 있는 것은 이름과 서버지 키가 아니다.

`building-export-import.md` — "양쪽 '키 빼고'를 없앤 이유"

---

## 2026-08-17

**오버뷰 키 머리글의 폴드 — 기각을 뒤집었다**

- **있던 결정** — 폴드는 안 한다. 근거가 넷 적혀 있었다: 오버뷰는 읽는 화면이라 조용해야 하고,
  자식이 하나둘인 그룹이 대부분이라 접는 컨트롤은 의식일 뿐이며, 폴드 화살표는 컨트롤이라 눈을
  끌고, 빌려온 형태(`ClickBindingHeaderTemplate`)에 저쪽에 없는 기능을 얹는 것이라서.
- **소유자** — *"예전에 이걸 접었던 이유는 ... export창의 그룹헤더를 넣어보고 화면 확인했었는데
  그 헤더는 색상부터가 너무 도드라져서 기각했었고 지금 헤더는 적당한 것 같아."*

**소유자.** 나는 적힌 넷 중 둘이 죽었다는 것까지 짚었다 — 머리글을 `ListHeaderThreeSliceTemplate`로
바꾸면 접힘을 말하는 것이 따로 붙는 컨트롤이 아니라 **오른쪽 끝 조각**이라, "컨트롤이 눈을 끈다"도
"저쪽에 없는 기능"도 성립하지 않는다. 소유자는 그보다 앞선 사실을 댔다: **그 넷은 애초에 기각
사유가 아니었다.** 실제로 문 것은 화면이었다 — 퀘스트 로그 머리글을 넣어보고 색이 너무 세서
물렀고, 적힌 근거들은 그 뒤에 붙은 말이었다. 아트가 바뀌자 기각이 통째로 근거를 잃었다.

문서에 적힌 사유가 결정을 낸 사유와 다를 수 있다는 것이 이 건의 값이다. 넷을 하나씩 따지는
동안 답은 "그때 무엇을 보고 정했나"에 있었다.

`reworking-the-overview.md` — "접기"

---

**도달불가의 등급 기준 — "고칠 것이 이 행에 있나"가 안 통했다**

- **나** — `UNREACHABLE`은 회색이고 근거는 *고칠 것이 이 행에 없다*이다. 이 행은 멀쩡한데 이웃이
  이겨서 밀린 것이므로, 이 코드가 `"key"` 갈래에 앉아 단축키 칸을 빨갛게 하는 지금 배치도 틀렸다.
- **소유자** — *"unreachable은 키와 관계가 있지 않나? 다른 키에 들어간다면 말짱해질 액션인데."*

**소유자.** 고칠 것은 이 행의 키에 있다. 등급은 회색 그대로인데 **근거가 무너졌고**, 근거 없는
등급표는 다음 코드가 늘 때 매길 자가 없다. 판정 쪽으로 다시 썼다 — *이 행 하나만 보고 판정되면
빨강, 이웃을 봐야 판정되면 회색.* 갈래 배치가 틀렸다던 것도 같이 접었다.

`grading-binding-issues.md` — "등급을 가르는 기준"

---

**오버뷰 사유 칸의 코드별 문구 — 총칭으로 되돌렸다**

- **있던 결정** — 총칭 `Has a Problem` 하나를 코드별 짧은 문구(`ORDER_FLAG_<코드>`)로 갈랐다.
  근거는 `GetShortIssueText`의 주석에 있다: 총칭은 **무엇을 고칠지를 안 말하고**, 툴팁을 열어야만
  알 수 있는 것을 그 칸이 이미 차지하고 있었다.
- **소유자** — *"groups_none_selected는 뜬금없다. 너무 세부적인 정보를 담고 있어서 이게 뭔가
  싶다. 툴팁에 세부 정보는 유지하되 오버뷰에서는 has issues 같은걸로."*

**소유자. 통째로 뒤집혔다.** 그때는 세부를 **어디서** 읽느냐가 문제였는데, 이번에 문 것은 그 칸의
해상도가 목록의 해상도와 안 맞는다는 것이다. 세부는 없애지 않고 툴팁 하나로 모은다. 나는 그 직전에
`*_NONE_SELECTED` 넷을 한 코드로 합치자는 안에 반대하면서 **"합치면 사유 칸이 구체성을 잃는다"**를
주된 근거로 들었는데, 이 결정이 그 근거를 없앴다 — 합치기는 다른 이유로 접혔다.

`grading-binding-issues.md` — "사유 칸은 총칭으로, 툴팁은 그대로"

---

**미번역 로캘 — 검사에서 뺐다**

- **있던 결정** — 로케일에 빠진 키는 검사 실패였고, 영영 없는 게 맞는 것(`ALLOWED_MISSING`)과
  번역 대기(`PENDING_TRANSLATION`) 두 명단으로만 면제됐다. 근거는 `check-locales.js` 머리말에
  적혀 있었다: 빠진 키는 조용히 나가고, 한 번 손으로 맞춘 것은 다음 커밋에서 다시 벌어졌다.
- **소유자** — *"enus를 제외한 다른 로캘은 중요하지 않고 급하지 않다. 마땅한 텍스트가 떠오르지
  않으면 아예 제외하고 영어로 폴백하게 한다. 미번역 로캘 체크는 수동으로만 한다. 화이트리스트인지
  검사는 뺀다(매번 추가하는 부담)"*

**소유자. 통째로 뒤집혔다.** 명단이 지키려던 것은 빠진 키를 잊지 않는 것인데, 빠진 키가 화면에
내는 것은 영어 한 줄이고 그건 이미 답이다. 새 문자열마다 두 번째 편집을 요구한 대가로 남은 것은
세어보면 알 수 있는 숫자 하나였다. 검사는 이제 로케일이 **담고 있는 것**만 본다 — 잔재 키, 중복,
서식. 안 담은 것은 세어서 찍고 통과하며, 목록은 `-- --missing`으로 부를 때만 나온다.

`writing-user-facing-text.md` — "enUS is the only file that has to be complete"

---

**합성 키 번호 — 전역 보관으로, 표기는 없앴다**

- **있던 결정** — 번호는 저장하지 않고, 부를 때마다 저장소 전체에서 `최댓값 + 1`을 뽑았다. 근거는
  `NextSyntheticKey` 주석에 있었다: 대기 중인 뭉치가 전부 진짜 키를 받으면 다시 1부터 시작하므로
  번호가 작게 유지되고, 화면에 나란히 선 둘이 #1과 #2로 읽힌다.
- **나** — 보관에 반대했다. 감소 경로가 없으면 번호가 영영 커져서 아무것도 남지 않은 화면에
  "받아온 지정 #47"이 뜬다. 작게 유지하려면 숫자 키가 사라지는 자리마다(받아들이기, 액션 삭제,
  배치 폐기, 해제, 임포트) 카운터를 되돌려야 하는데 `최댓값 + 1`은 되돌릴 것이 없다.
- **소유자** — *"숫자가 이미 사용자에게 노출되는 값이 아닌 이상 커져도 상관 없고... 필요한 경우
  언제든 압축할 수 있다."*

**소유자. 프레임 밖에서 왔다.** 내 근거는 전부 "번호가 작아야 한다"에 서 있었고, 그건 번호를
찍는다는 전제 위에 있었다. 머리글이 이미 "첫 액션 +N"으로 뭉치를 부르고 있으므로 번호는 부를
이름이 아니라 보관 방식이다. 표기(`KEY_GROUP_IMPORTED`)를 없애는 결정이 내 반대 근거를 통째로
없앴다. 나는 그 앞에서 "저장하면 진실이 둘이 된다"고도 했는데, 카운터도 SavedVariables 안이고
그게 유일진실이라 그 말은 성립하지 않았다.

같은 자리에서 딸려 나온 것: **액션 둘 이상인 그룹을 해제하면 키를 지우는 게 아니라 합성 키를
준다.** `key == nil`인 액션은 어느 그룹의 것도 아니라, 지우면 넷이 낱개 넷이 되고 한 벌이었다는
것을 적어두는 데가 없다. 해제가 임포트에 이어 두 번째 발급자가 됐고, 그것이 보관을 물은 이유였다.

`reworking-the-overview.md` — "키 없는 그룹은 번호를 안 보여준다"

---

**그룹 머리글의 우클릭 메뉴 — 기각을 뒤집었다**

- **있던 결정** — 머리글에서는 안 연다. 근거는 발견 가능성 하나였다: 우클릭은 컨트롤을 안 늘리는
  대신 **아무 표시가 없어서 아무도 못 찾는다**, 행 우클릭은 적어도 툴팁이 가리키고 있다. 나란히
  선 다른 줄(머리글에 버튼)은 띠에 위젯을 얹게 되어서 기각했고, 그 둘로 머리글 자리를 통째로
  닫았다.
- **소유자** — *"오버뷰 프레임 그룹헤더... 우클릭 드랍다운 메뉴 추가. 해당 그룹의 액션에 대해
  한번에 단축키 지정. 현재 set keys for all of these라는 오버뷰메뉴 액션행 드랍다운
  메뉴아이템이 같은 기능"*

**소유자. 통째로 뒤집혔다.** 발견 가능성 문제는 **답해서 없앤 것이 아니라 무게가 밀린 것**이다 —
행 메뉴가 그대로 남아 그 몫을 계속 지고, 머리글은 아는 사람이 그룹을 직접 겨누는 두 번째 입구가
된다. 소유자가 같은 항목이 행 메뉴에 이미 있다는 것을 짚으면서 지시한 것이 그 모양이다. 내가 든
사유가 겨눈 것은 자리가 아니라 표시였고, 그러고도 나는 자리를 닫았다 — 같은 표에서 **키 그룹이
서 있는 곳은 머리글**이라고 내가 쓴 채로. 덤으로 사실 하나가 틀려 있었다: "두 번째 길은 위젯을
얹는 것"이라고 적었는데 우클릭은 위젯을 안 늘린다.

`reworking-the-overview.md` — "그룹 머리글의 우클릭 메뉴"

---

**머리글 메뉴의 라벨 — 범위를 라벨에 넣었다가 다시 뺐다**

- **나** — 라벨은 「단축키 지정」으로 짧게, **한 벌 전체에 걸린다는 말은 툴팁이 진다.** 근거는
  자리였다: 그 한 벌의 띠를 직접 눌러서 연 메뉴이고 제목이 그 한 벌을 안에 든 것으로 부르므로,
  라벨이 범위를 다시 말할 필요가 없다. (이 줄 앞에 내가 행 메뉴의 문자열을 그대로 갖다 쓴 일이
  있고 소유자가 잡았는데, 그건 이미 적혀 있던 규칙을 내가 어긴 것이라 여기 적을 것이 아니다.)
- **소유자** — *"그렇다고 '벌크'/해당 그룹 전체라는 느낌을 쏙 빼버리네"* — 그래서 라벨을
  *"이 아래 있는 것 전부에 단축키 지정"*으로 늘렸다. 그 다음: *"메뉴 라벨은 다시 돌려줘.
  툴팁으로 벌크라는걸 표시하도록 하자. 메뉴가 너무 길어지니까."*, *"(니가 맞았고 내가 틀렸다)"*

**소유자가 자기 결정을 뒤집었다. 프레임 밖에서 왔다.** 둘 다 **뜻**을 놓고 다투고 있었다 — 그
절이 빠지면 무엇으로 읽히나. 답을 낸 것은 **폭**이다: 컨텍스트 메뉴는 제일 긴 줄만큼 넓어지고 이
메뉴는 줄이 둘뿐이라, 그 라벨이 곧 메뉴 폭이 된다. 내 라벨이 살아남았지만 내가 든 근거로 살아남은
것은 아니다 — 나는 그 자리를 화면으로 본 적이 없어서 폭을 근거로 대지 못했다.

`reworking-the-overview.md` — "그룹 머리글의 우클릭 메뉴"

---

> **소유자** — *"그룹 헤더는 '선택' 상태가 없기 때문에, 이동이 되면 그게 어느것인지 찾기 힘들어."*

**그룹째 reveal — 만들었다가 같은 날 뺐다.** 다투던 것은 **어디로 스크롤하나**였다. 캡처
다이얼로그의 [키 설정 해제]가 왼쪽 열을 안 따라가는 것을 소유자가 짚었고, 그 자리에 그룹을
부탁하는 입구(`RequestRevealKeyGroup`)와 머리글을 세우는 스크롤러(`RevealGroup`)를 만들었다.
근거는 "한 벌이 움직였으면 답은 한 벌이지 그 안의 한 행이 아니다"였다.

**틀을 바꾼 것은 스크롤이 아니라 표시다.** 도착해도 어느 머리글인지 못 고른다면 스크롤은 답의
절반만 낸 것이고, 이 열이 "이거"라고 말할 수 있는 표시는 행에 하나뿐이다. 머리글에 그것을 새로
다는 것은 선택 상태를 둘로 만드는 일이라 — 이 창의 선택은 상세 패널과 벌크가 읽는 값이고 머리글은
액션이 아니다 — 두 안 중 남은 것은 **첫 행 reveal**이었고, 그러면 `RevealRow`가 머리글을 데려오는
덕에 스크롤 문제도 같이 풀린다. 만든 셋을 도로 뺐다.

딸려 나온 것: 그 구멍의 원인은 해제 쪽이 `RebuildAfterKeyGroupChange`를 **손으로 옮겨 적은**
네 줄이었고 한 줄이 빠져 있었다는 것. 지금은 두 갈래가 그 함수 하나를 부른다.

`reworking-the-overview.md` — "한 벌이 통째로 움직여도 부탁하는 것은 행이다"

---

> **소유자** — *"Keep them이 아니지. Keep both나 Keep all이어야하지. keep them은 느낌상 취소잖아."*

**차 있는 키 확인창의 두 답 — 낱말을 갈았고, 한 번 기각했던 짝이 돌아왔다.** 나는 축으로 골랐다:
본문이 묻는 것이 *"그것들은 어떻게 할까요"*이니 두 버튼 다 **점유자**에 대해 답해야 한다고, 그래서
`Keep them` / `Unbind them`을 냈다.

**소유자. 프레임 밖에서 왔다.** 내가 문장 안에서만 읽는 동안 소유자는 **버튼이 서 있는 자리**를
봤다 — [취소] 바로 옆의 `Keep them`은 "그것들을 그대로 둬라", 곧 아무것도 하지 마로 읽힌다. 축을
맞추려던 낱말이 하필 세 번째 버튼과 같은 뜻으로 보이는 자리였고, 그 오독은 옆 버튼이 지정을
걷어내는 창에서 제일 비싸다. `both`가 새로 오는 쪽을 문장 안에 끌고 들어와 그것을 막는다.

딸려 나온 것: 그렇게 고른 `Keep both` / `Unbind them`은 `building-export-import.md`가 **한 번
기각한 그 짝**이다. 기각 사유(두 버튼의 축이 갈린다)는 지금도 맞지만, 대신 세웠던 합치기/덮어쓰기도
같은 불일치를 갖고 있었고 둘 다 클라이언트에 없는 낱말이었다. **축 불일치를 알고 받아들였다.**

`building-export-import.md` — "질문의 문구 (`KEY_GROUP_CONFLICT`)"

## 2026-08-18

> **소유자** — *"이 툴팁 함수는 내용을 빌드하는 역할로 제한하자는거고 그걸 어디에 붙일지는
> 호출하는 쪽에서 결정하게 하면 인자 두개를 그냥 뺄 수 있다는건데"*

**액션 툴팁을 내용 함수로 자른다 — 내가 두 번 미뤄둔 것을 소유자가 뒤집었다.**

- **나** — 통짜로 둔다(`ShowActionTooltip(owner, anchor, action, opts)`). 근거 둘이었다. 하나,
  내용만 붙이는 함수를 따로 내면 **쓰는 데가 없다.** 첫 사용자로 짚었던 임포트 미리보기는 행
  목록조차 아직 없다는 것을 확인했다. 둘, 쪼개면 호출부가 `Show`를 빠뜨렸을 때 아무것도 안 뜨는
  **조용한 실패**가 생긴다.

**소유자. 프레임 밖에서 왔다.** 나는 "그 헬퍼를 오늘 누가 쓰나"를 묻고 있었고, 소유자는 **"이
함수의 일이 어디까지인가"**를 물었다. 내용을 짓는 데까지가 이 함수의 일이면 어디에 붙는지는
부르는 쪽 것이고, `owner`와 `anchor`는 그냥 빠진다.

그 자리에서 내 근거 둘 다 무너졌다. 첫째는 **다른 안을 겨누고 있었다.** 나는 통짜 함수를 남긴
채 내용 함수를 하나 더 두는 안을 깠는데, 소유자 안은 함수가 하나고 그것이 내용 함수다. 호출부
넷이 전부 쓴다. 둘째의 "조용한"은 틀린 낱말이었다. 호버해도 툴팁이 안 뜨는 것은 UI에서 낼 수
있는 가장 시끄러운 실패고, 이 리포가 조용하다고 부르는 것은 아무도 못 보는 쪽이다.

딸려 나온 것: 내가 지키려던 캡슐화가 애초에 없었다. 통짜 함수는 `SetOwner`와 `SetMinimumWidth`와
`Show`를 쥐면서 **뒷정리는 호출부에 남긴다.** 익스포트 행의 `OnLeave`가 폭을 안 되돌리고 있는
것이 그 비대칭이 실제로 낸 값이다.

`sharing-one-action-tooltip.md` — "시그니처"

---

> **소유자** — *"이건 그냥 아무것도 하지마. char도 막지마. 우리가 막을 일이 아니고 우리
> 애드온에서 안터지는 문제다."* / *"잘못된 문자열을 복사해서 나가도 우리가 들어오는걸 제대로
> sanitize한다면 안터지는 문제잖아."*

**복사 상자의 편집 막기 — 서 있던 결정이 통째로 뒤집혔다.**

- **있던 결정** — 여기서는 아무도 못 고친다. 근거는 코드에 적혀 있었다: *"An edited string is a
  broken string, and the break would only show up on whoever it was pasted to."* 고친 문자열은
  깨진 문자열이고, 깨진 것은 붙여넣은 **남**에게만 드러난다.
- **나** — 그 가드가 `OnChar`만 덮어서 백스페이스와 Delete가 새는 것을 결함으로 적었다. 열자마자
  전체가 선택돼 있으니 백스페이스 한 번이면 빈 문자열을 복사한다. **가드를 마저 채우자는
  쪽이었다.**

**소유자. 프레임 밖에서 왔다.** 나는 "이 가드가 새는가"를 물었고, 소유자는 **"이 가드가 무엇을
지키는가"**를 물었다. 답은 아무것도다. 절반을 채워도 잘린 붙여넣기와 손으로 만든 문자열은 그대로
들어오므로, **임포트는 어차피 아무 문자열에나 안전해야 한다.** 그러고 나면 편집된 문자열은
임포트가 되돌려보내는 여러 문자열 중 하나일 뿐이고, 나가는 쪽에 가드를 둘 이유가 없다.

내 근거가 틀린 것이 아니라 **재는 자리가 틀렸다.** 새는 것은 맞는데, 안 새게 만들어도 값이 없다.

딸려 나온 것: 이 결정이 `fixing-what-the-review-found.md`의 임포트 sanitize 항목들에 무게를
옮긴다. 나가는 쪽을 안 지키기로 한 이상 들어오는 쪽이 그 몫을 다 진다.

`fixing-what-the-review-found.md` — "무엇부터"

---

> **소유자** — *"ui는 코드의 view일뿐이야. 개념은 import<>export가 맞다."*

**`DebindStorage`의 파일 경계 — 쪼개자던 것을 합치기로 뒤집었다.**

- **나** — `Drawer.lua`가 세 가지를 하고 있으니 **쪼개자**. 형식을 읽는 쪽과 배치를 보관하는 쪽으로.
  근거는 "파일 이름이 자기 마지막 3분의 1만 설명한다"였다.
- **소유자** — 합쳐라. 와우의 로드아웃도 다른 애드온의 설정도 전부 import↔export 둘뿐이고 중간에
  bring이 끼지 않는다. 우리 사정 때문에 임포트를 둘로 갈랐을 뿐이며, **개념은 여전히 둘이어야 한다.**

**소유자. 프레임 밖에서 왔다.** 나는 파일이 무엇을 담고 있나를 세고 있었고, 소유자는 **무엇이
개념이고 무엇이 그 개념의 view인가**를 물었다. 서랍은 개념이 아니라 임포트가 여러 번에 걸쳐
일어나서 중간 상태를 저장한다는 사실이고, 그러면 파일도 개념대로 `Import.lua` 하나여야 한다.
쪼개는 안은 view를 개념으로 대접한 채 그 안에서 선을 다시 그은 것이었다.

같은 착각이 이름에도 있었다. 내가 새 파일 이름으로 민 `Batches`는 "코드가 쓰는 명사"라고 골랐는데,
서랍이 목록이라 배치가 행으로 보여서 생긴 낱말이다. **화면에서 나온 이름을 코드의 이름으로 읽었다.**

**서랍 UI는 그대로 둔다.** 대신할 화면이 아직 없다는 것이 이유이고, 이번 결정은 코드가 그것을
개념으로 취급하는 부분에만 닿는다.

딸려 나온 것: 저장 형식이 열렸다. 문자열을 저장할 이유로 내가 넷을 댔고 넷 다 코드를 따라가 보니
성립하지 않았다. 아직 결정된 것은 없다.

`putting-import-back-together.md`

---

> **소유자** — *"문자열이 필요하면 역직렬화된걸 다시 직렬화 하면 되지 않나"* /
> *"니가 논거를 대려면 문자열 저장할땐 되는데 페이로드로 저장하면 안되는 경우를 따져야지"*

**서랍은 페이로드를 저장한다 — 그리고 그 결론보다 방법이 값지다.**

- **나** — 원문 문자열을 저장하는 데는 이유가 있을 것이다. 넷을 댔다. 되돌리기가 그 위에 선다,
  사용자가 도로 복사해 갈 수 있다, 나중에 새 디코더로 다시 읽을 여지가 있다, 남의 형식이라
  우리가 마이그레이션 안 해도 된다.
- **소유자** — 넷 다 문자열이어야 할 이유가 아니다. **"A는 되는데 B는 안 되는 경우"를 대라.**

**소유자. 프레임 밖에서 왔다.** 내가 댄 것은 전부 **양쪽 다 참인 것**이었다. 그 잣대를 받으니 넷이
한 번에 정리됐다. 되돌리기는 페이로드로도 된다. 도로 복사해 나가는 **통로가 리포에 없다.** 못 읽는
문자열은 `AddBatch`가 저장 전에 거절하므로 서랍에 든 것은 전부 이미 이해한 것이다. 그리고 넷째는
방향이 반대였다 — 디코더가 버전이 다르면 거절하므로 **스키마가 오르면 마이그레이션이 면제되는 게
아니라 봉쇄된다.**

정보도 같다. `DecodeExportString`은 역직렬화 결과를 손대지 않고 돌려주고 버전은 `payload.v`로
같이 간다. 남은 차이는 디스크 크기 하나였고, **재지 않기로 했다** — 작지만 못 읽는 것을 들고 있는
쪽이 나쁘다.

**내가 반복한 실패가 이 건에 다 있다.** 지금 코드가 이러니 이유가 있겠지에서 출발해 이유를
지어냈고, 코드가 아니라 주석을 근거로 삼았다(`도로 복사해 갈 수 있다`가 그 주석이고 통로는 없다).

`putting-import-back-together.md` — "할 일 2"

---

> **소유자** — *"매크로는.... 함부로 공유해서는 안되는 데이터를 들고 있을 수 있다"* /
> *"블리자드 매크로의 내용을 담아서 커스텀매크로로 생성 이건 빼는게 낫지 싶다."*

**매크로 본문은 문자열에 안 싣는다 — 서 있던 기능이 통째로 빠졌다.**

- **있던 결정** — `MACRO`는 `macro = { name, body, icon, scope }` 스냅샷을 같이 보낸다. 근거는
  `MacroMatches`에 적혀 있었다: 이름·스코프·내용 삼중 일치를 보면 **자기 백업을 다시 임포트할 때
  납작해진 텍스트가 아니라 `MACRO`를 돌려받는다.**
- **나** — 임포트가 그 스냅샷을 화이트리스트 없이 옮기는 것이 문제라고 적었다. 고칠 모양은
  **그 세 줄도 같은 문을 지나게 하는 것**이라고 했다.
- **소유자** — 본문 자체를 보내지 마라.

**소유자. 프레임 밖에서 왔다.** 나는 **검증**을 보고 있었다. 타입이 맞나, 문을 지났나. 소유자는
**그 값이 애초에 나가도 되는 것인가**를 물었다. 매크로 본문은 사용자가 자유롭게 쓴 텍스트고 무엇이
들어 있는지 우리가 모른다. 그리고 **보내는 사람이 그것이 나가는 줄 모른다** — 그는 액션이 자기
매크로 `X`를 부른다고 알고 있다.

내가 고치려던 세 줄이 그 결정으로 **통째로 없어진다.** 검증을 단단히 하는 것과 값을 안 보내는
것은 다른 층위였고, 나는 위층을 안 봤다.

받는 쪽 답은 이미 있었다. `GetMissingMacroName`과 `BINDING_ISSUE_MISSING_MACRO`. 그 검사의
머리주석이 *"문자열이 설치본 사이를 오가기 시작한 뒤로는 뺄 수 없게 됐다"*고 적어놨다. 본문
폴백은 그 위에 얹혀 있던 것이다.

잃는 것도 적어둔다. **자기 백업을 매크로 지운 뒤 복원하는 경우.** 받아들였다 — 본문이 실제로
쓰이는 것은 이름이 안 맞을 때뿐이고, 그때가 바로 남의 본문이 내 프로필에 앉는 순간이다.

`building-export-import.md` — "★ 매크로는 이름만 나간다"

---

> **소유자** — *"type을 3개로 나누는건?"* /
> *"오히려 하나의 타입에서만 인식되는 필드를 넣는게 오히려 이상하지 않니"*

**`SETSTATE`가 타입 셋으로 갈린다.** `setstate_on` / `setstate_off` / `setstate_toggle`,
`value`는 이름 하나.

- **나** — `value`를 문자열로 바꾸고 모드와 상태를 한 필드에 적는다(`"on:$state3"`). 구분자를
  정하면 된다.
- **소유자** — 타입을 셋으로 갈라라. 이어서 `value2`를 물었고, 내가 검증면이 는다고 답하자
  **한 타입만 아는 필드가 오히려 이상하다**로 접었다.

**소유자.** 내 안은 접힌 값을 문자열로 펴는 데서 멈췄다. 소유자 안은 **`type`이 동사고 `value`가
목적어**라는 이 리포의 모델로 되돌린다. `SETSTATE`만 동사를 값 안에 접고 있었고, 카탈로그가 열다섯
항목을 만드는 것이 그 증거다 — 사용자는 상태를 고르고 모드를 고르는 게 아니라 하나를 고른다.

그리고 **계약이 안 넓어진다.** `mode` 필드였으면 `SETSTATE`가 아니면 뜻이 없는 필드가 모든 액션이
지나는 화이트리스트에 한 줄로 선다. `type`은 원래 값의 뜻을 정하는 자리라 셋이 되어도 표가 그대로다.

딸려 나온 것: 임포트가 만든 `SETSTATE`가 값 없이 돌아다니다 `band(nil, …)`으로 터지던 것이
**원인째 없어진다.** 리뷰 목록에서 그 항목을 빼고 이 결정으로 넘겼다.

`.zzz/custom-states-redesign.md` — §9-1

---

> **소유자** — *"와우의 매크로는 절대 index나 숫자로 보관 사용하면 안 돼. 이건 완전 조용히 크게
> 깨진다."* / *"숫자값은 영속되는 값이 아니다. 이름순 정렬되어서 그 매크로보다 앞 매크로가
> 삭제되거나 추가되면 번호가 바뀐다."* / *"순전히 내 클라이언트에만 존재해도 깨지는 거다."*

**매크로 참조는 어느 순간에도 이름이다 — 내가 익스포트 문제로 보던 것을 액션의 규칙으로 옮겼다.**

- **나** — 본문 스냅샷을 걷어내면 옛 데이터의 슬롯 번호가 맨 숫자로 나가니, 내보낼 때
  `GetMacroInfo`로 이름을 떠서 싣자. 스냅샷이 막고 있던 것이 본문 폴백만은 아니었다.
- **소유자** — 그건 익스포트의 문제가 아니라 `type = macro`가 지켜야 하는 규칙이다. 그리고 숫자는
  이름으로 못 되돌린다.

**소유자.** 내가 이 문제를 통째로 **"설치본 사이"라는 틀** 안에서 보고 있었다. 슬롯 번호를 남이
읽으면 위험하다고. 소유자는 번호가 참조가 아니라 **이름순 목록에서의 자리**라는 것을 짚었고, 그러면
공유가 하나도 없어도 깨진다 — 앞에 정렬되는 매크로를 하나 만든 다음날, 같은 계정 같은 캐릭터에서.
익스포트는 이 문제가 드러난 자리였지 문제가 있는 자리가 아니었다.

그 틀에서 보면 내 변환이 왜 틀렸는지도 같이 보인다. `GetMacroInfo(4)`가 답하는 것은 **지금 이
저장소의 4번**이라, 이름으로 바꾸는 것은 복구가 아니라 추측이고, 추측한 값은 멀쩡한 얼굴로 남의
매크로를 시전한다.

**그리고 내가 막으려던 그 "옛 데이터"는 존재한 적이 없다.** 최초 커밋(2024)부터 커서 드롭이
`GetMacroInfo`로 이름을 떠서 넣고 있었다. 나는 주석 한 줄을 근거로 삼아 없는 데이터를 위한 변환을
짓고 테스트까지 붙였다.

**임포트가 그것을 어떻게 거절하느냐도 소유자가 한 번 더 꺾었다.** 내 첫 구현은 그런 액션을 페이로드
훑는 자리에서 **액션으로 안 쳤다**. 세는 자리와 놓는 자리가 같은 목록을 봐서 개수가 안 어긋난다는
것이 근거였다.

> **소유자** — *"조용하게 액션이 사라지는 건 틀린 답이다. 성공하되 액션에 에러를 띄우든지 임포트
> 자체가 실패해야지."*

**소유자.** 개수를 맞추려고 받는 사람이 알 기회를 없앴다. 한 키에 걸린 묶음이 반만 들어와도 온전한
줄 알고 승인한다. 그래서 다음 안은 **값만 떨어지고 액션은 들어오는 것**이었다.

**그 안의 근거로 내가 든 것이 거짓이었다.** *"조건·키·우선순위가 그대로 살아서 받는 사람이 매크로만
다시 지정하면 된다"* — 코드를 따라가 보니 **기존 액션의 대상을 바꾸는 길이 이 애드온에 없다.**
드롭은 언제나 새 액션을 만들고, `action.value`에 쓰는 자리는 매크로 본문 편집기와 그 취소뿐이다.
받는 사람이 할 수 있는 일은 삭제 하나였고, 그 사실이 아래 항목의 통째 거절로 이어졌다.

`devdocs/building-export-import.md` — "우리가 만들 수 없는 것이 하나라도 있으면 문자열을 통째로 거절한다"

---

> **소유자** — *"그냥 sanitize에서 실패하면 깔끔하게 import 전체 실패하는쪽으로 기운다"* /
> *"그냥 통째 거절로 정했다"* / *"우리 애드온이 만들 수 없는 문자열을 직접 만들어서 거기서 오류가
> 났다면 나머지도 믿을건 못된다."*

**검증에 걸린 페이로드는 통째로 거절한다.**

**뒤집힌 것: "액션은 들어온다. 값만 빠진다."** 바로 위 매크로 항목이 그날 정한 것이고, 그 근거는
"화이트리스트가 다른 모든 잘못된 값에 하는 것과 같다"였다. 이제 화이트리스트에 하나라도 걸리면
페이로드 전체가 거절되고, 배치가 만들어지지 않는다. 사유는 `IMPORT_FAILED_DAMAGED` 하나다.

- **나** — 부분 수용을 지켰다. 서 있는 규칙이 그것이고("깨진 것도 보내고 읽는 쪽이 빨간 걸 보고
  지운다"), 통째로 버리면 보낸 사람과 받는 사람이 다른 개수를 본다.
- **소유자** — 필드마다 정확한 오류를 내주기는 힘들고, 그걸 안 하면 받는 사람이 고칠 수 있다는
  보장이 없다. 깔끔하게 전체 실패가 낫다. 그리고 **한 군데가 우리가 만들 수 없는 모양이면 그
  문자열은 우리 익스포트에서 나온 것이 아니고, 나머지도 보증이 없다.**

**소유자.** 내가 규칙을 그 규칙이 지키려던 것과 분리해서 들고 있었다. 부분 수용은 **버전이 다른
발신자를 견디는 장치처럼** 읽히는데, 그 일은 `DecodeExportString`의 스키마 검사가 이미 한다 —
높아도 낮아도 통째로 거절이라 버전이 다른 우리 문자열은 화이트리스트까지 오지도 못한다. 그러면
화이트리스트에 걸리는 문자열은 **손으로 만들었거나 변조된 것뿐**이고, 부분 수용이 봐주고 있던
대상이 정상 발신자가 아니었다. 지키던 것이 없으니 값이 없다.

**그리고 "받는 사람이 고친다"가 성립한 적이 없다.** 그 문자열은 그 사람이 쓴 것이 아니다. 조건
하나가 조용히 빠진 액션은 목록에서 멀쩡해 보이고 누를 때만 다르게 나가는데, 빨개지는 것은 매크로
이름 빠진 것 정도고 나머지는 아무 표시가 없다. 부분 수용의 이득은 처음부터 없었고 대가만 있었다.

**내가 여기서 한 번 더 틀렸다.** 사유 문자열에 어느 필드가 걸렸는지 얹자고 했다 — *"보낸 사람한테
전할 말이 된다"*. 소유자가 *"이걸 보내서 보낸 사람이 직접 lua테이블을 편집해서 직접 직렬화하라는
거냐"*로 잘랐다. 손으로 만든 사람은 이미 코드를 읽고 있고, 아닌 사람에게는 **아무도 수행할 수 없는
지시**다. 게다가 `key` 같은 코드의 낱말을 사용자 화면에 올리는 것이라 `writing-user-facing-text.md`가
금지하는 자리였다.

**딸려 닫힌 것.** 리뷰 목록의 "손으로 만든 문자열이 마우스만 올려도 터진다"(`batch.class`)와
"NaN 하나가 붙여넣기를 에러로 만든다"가 둘 다 이 문 하나로 없어진다. 저장되기 전에 거절되므로
`batch.class`가 디스크에 앉지 않고, 개수 루프가 NaN을 인덱스로 쓰는 자리에도 도달하지 않는다.

`devdocs/fixing-what-the-review-found.md` — "거부하면 페이로드가 통째로 떨어진다"

## 2026-08-19

> **소유자** — *"사용자가 쓰지도 않는 바인딩을 나중에 참고하려는 용도로 게속 들고 있을거라는게
> 마음에 안들어."*

**받아온 그룹의 머리글이 온 키를 말한다. 그 키는 승인하는 순간 사라진다.**

- **나** — 필드를 가르자고 했다. `imported`는 검역 배지로만 두고 보낸 쪽 키는 새 필드에 남기는
  안이다. 근거는 [%d개 모두 받기]가 키를 안 주고 승인하는 정식 경로라는 것이었다. 승인 뒤가
  키를 정하는 구간인데 그 단서가 하필 그때 없어진다.
- **소유자** — 사용자가 쓰지도 않는 바인딩을 나중에 보여주려고 프로필이 계속 들고 있는 것이
  싫다.

**소유자. 프레임 밖에서 왔다.** 나는 "그 값이 언제 쓸모 있나"를 재고 있었고, 소유자는 "프로필이
그것을 저장할 자격이 있나"를 물었다. 앞엣것은 화면 편의고 뒤엣것은 저장 정책이라 뒤엣것이
먼저 선다. 그래서 `From {키}`는 배지가 살아 있는 동안만 서고, 승인하면 클라이언트의
`NOT_BOUND`로 돌아간다. 새 필드도 `KEYS_TO_SAVE` 한 줄도 없다.

**같이 접힌 것.** 이걸 받아온 것에만 한정할 이유가 있냐는 물음이 나왔다. 사용자가 자기 그룹을
해제했을 때의 옛 키도 같은 자리라는 것이다. 내가 필드 둘과 낱말 둘과 덮어쓰기로 흩어진 액션까지
펼쳐놓자 소유자가 *"너무 과해진다"*로 접었다. 문은 열려 있고, 다시 꺼낼 때 필요한 것은 오늘
나왔다. 두 경우는 낱말이 갈리는데(FROM과 WAS) 승인 뒤에는 데이터로 구분이 안 되므로, 하나로
합친 필드로는 안 되고 둘이어야 한다.

`devdocs/reworking-the-overview.md` — "받아온 그룹은 온 키로 부른다"

> **소유자** — *"오버뷰목록에서 가져온 액션에 대해서도 ASSIGN A KEY 메뉴 표시"*

**왼쪽 열의 행 메뉴가 배지 붙은 행에도 [단축키 지정]을 세운다. 서 있던 결정이 뒤집혔다.**

안 세우던 사유는 두 가지였다. 도착한 한 벌에서 그 줄 하나만 떼어 낸다는 것, 그리고 키를 정하면
그 줄만 승인된다는 것. 둘 다 지금도 사실이다. 안 세울 이유가 못 되는 쪽이 바뀌었다. 한 벌에서 한
줄을 떼는 것은 이 메뉴가 다른 데서 이미 내주는 조작이고 그 경고는 툴팁이 지고 있으며, 키를 정하는
것이 곧 승인이라는 것은 이 애드온의 규칙이라 이 항목이 몰래 끼우는 것이 아니다.

**같은 항목이 통(레이어 탭) 쪽에는 2026-08-17에 이미 섰다.** 그때 근거가 "왼쪽 열에는 대신 갈 곳,
즉 머리글이 있고 통에는 없다"였는데, 그 뒤로 두 열이 같은 행에 다른 메뉴를 내주고 있었다.

`devdocs/reworking-the-overview.md` — "행 메뉴는 행 하나만 겨눈다"

> **소유자** — *"왼쪽 목록에서 액션에 대해 ACCEPT나 키지정을 할때 그 액션을 선택하는 것과 같은
> 결과가 나와야 된다고 생각 안하냐?"*

**행에 손댄 결과는 그 행을 고른 결과와 같다.**

- **나** — 그 행이 목록에서 자리를 옮기는 조작에만 필요하다고 봤다. 키 지정은 왼쪽 열에서 다른
  그룹으로 건너뛰니까 따라가야 하고, 승인은 배지만 꺼지고 자리는 그대로니 필요 없다.
- **소유자** — 왼쪽 열의 행을 좌클릭하면 창이 그 액션의 탭까지 따라간다(`GoToAction`). 같은 행의
  메뉴로 승인이나 키 지정을 했을 때 안 따라갈 이유가 없다.

**소유자. 프레임 밖에서 왔다.** 나는 "스크롤이 필요한가"를 재고 있었고, 소유자는 "조작이 끝났을 때
창이 무엇을 보고 있어야 하는가"를 물었다. 뒤엣것으로 물으면 자리가 안 옮겨졌다는 사실은 답과 상관이
없다. 다른 탭 것이면 오른쪽 패널은 여전히 엉뚱한 것을 보여주고 있다.

`devdocs/reworking-the-overview.md` — "다음에 볼 것"의 "행에 손댄 결과는 그 행을 고른 결과와 같아야 한다"
