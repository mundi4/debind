# 클라이언트의 자동 동작을 액션마다 정하기 (2026-09-21 시작)

> 상태: **설계만 섰다. 코드는 한 줄도 안 건드렸다.** 값의 모양(세 값), 기본값, 기존 프로필
> 처리까지 2026-09-21에 소유자와 정했고, 실행 경로가 이미 서 있다는 것도 확인했다. 남은 것은
> 화면 문구와 잠금 규칙의 세부, 그리고 구현이다.
>
> 이 문서는 `implementing-focus-and-self-cast.md` §3-13(보류)을 대신하고,
> `matching-the-clients-cast-targeting.md` §2-2의 결정 하나를 뒤집는다.

## 1. 무엇을 만드나

**액션마다 네 줄, 줄마다 세 값.**

| 줄 | CVar |
|---|---|
| 자신에게 자동 시전 | `autoSelfCast` |
| 자동 변신 해제 | `autoUnshift` |
| 자동 탈것 내리기 | `autoDismount` |
| 비행 중 자동 탈것 내리기 | `autoDismountFlying` |

세 값은 **켬**, **끔**, **게임 설정 그대로**이고 기본은 셋째다. 그 액션을 누르는 동안에만 그
CVar를 그 값으로 두고, 누름이 끝나면 되돌린다.

**네 CVar 전부 `SetCVar`로 움직인다** (2026-09-21, 소유자가 게임에서 확인). `autoUnshift`와
`autoDismount`는 인터페이스 코드에 이름이 없어서 설정 패널에도 안 올라오지만 실재한다. 코드에
안 보이는 것을 근거로 없다고 단정했다가 두 번 틀렸다.

## 2. 왜 세 값인가

**두 값으로는 적을 수 없는 것이 있다.** 게임 설정을 꺼 둔 사람이 이 액션에서만 켜는 것을 못
적고, 체크 안 함이 "게임 설정 그대로"인지 "끄지 않음"인지도 안 갈린다.

**칸이 생기면 안 켠 상태가 게임 그대로여야 한다** (2026-09-21, 소유자). 우리가 정한 값이 기본으로
서 있으면 칸은 켜는 물건이 아니라 잠금을 푸는 물건이 된다. 이것이 아래 §3을 강제한다.

## 3. 뒤집히는 결정

`matching-the-clients-cast-targeting.md` §2-2는 **대상을 지목한 주문과 아이템에서 자동 자가시전을
끈다**고 정했다(2026-09-11). 근거는 같은 설정 안에서 답이 갈린다는 것이었다. 포커스가 적이면
시전이 나가고 자가시전이 나에게 고치는데, 포커스가 없으면 `UnitExists` 가드가 버려서 아무 일도
안 난다.

**그 갈림은 클라이언트 자신의 것이다** (2026-09-21, 소유자가 게임에서 확인). 블리자드 액션바
버튼에 `unit`을 붙여 놓고 같은 것을 재면 똑같이 갈린다. 그러면 갈림을 없애는 것은 중립으로
돌아가는 것이 아니라 클라이언트에 없는 동작을 우리가 기본값에 얹는 것이다.

**그래서 자동으로 끄던 것을 걷는다.** 유닛을 고른 액션도 기본값에서는 액션바에 유닛을 붙인 것과
같이 동작한다. §2-2가 지키려던 "Target을 골라 놨는데 왜 나한테 나가느냐"는 이제 사용자가 켜는
값이 답한다.

**기존 프로필은 안 켠다** (2026-09-21, 소유자). 대상을 골라 둔 액션의 칸을 마이그레이션에서
켜 주지 않는다. 업데이트 한 번에 지금 아무 일도 안 나던 누름이 자기 자신 시전으로 바뀌는데,
그것이 맞는 동작이기 때문이다.

## 4. 실행 경로

**장치는 이미 서 있다.** 유닛이 실린 승자는 지금도 `UpdateBindings.lua`가 버튼마다 구워 둔
`-nosc` 쌍둥이로 나간다. 그 쌍둥이는 CVar를 저장하고 끄고 `/click`하고 되돌리는 매크로 본문이다.
바꿀 것은 셋이다.

**게이트.** `SecureBindings.lua`의 `SELFCAST_OFF_SNIPPET`이 지금은 `unit`이 있을 때 이 길을
탄다. 값이 하나라도 기본이 아닐 때 타는 것으로 바꾼다. 유닛 유무와 무관해진다.

**본문은 누를 때 굽는다.** 값의 조합마다 버튼마다 본문을 미리 굽는 것은 조합이 는다.
`BAKE_WINNER_MACROTEXT_SNIPPET`이 커스텀 매크로 본문을 이미 매 누름 이어붙여 속성에 쓰고 있으니
같은 자리를 쓴다. 되돌릴 전역은 `DebindAutoSelfCast` 하나가 아니라 CVar마다 하나씩 필요하다.

**겹이 아니라 줄이다.** 매크로 안에서 매크로는 안 돌아서, 고른 CVar마다 저장하고 쓰는 줄을 앞에,
되돌리는 줄을 뒤에 붙인 본문 하나가 된다. 가운데 `/click`은 하나다.

**`CastFrame`의 `unit`을 비워야 한다.** 지금은 유닛이 있을 때만 이 길을 타서 늘 쓰고 들어가는데,
유닛 없는 액션도 타게 되면 그 자리에 지난 누름의 대상이 남는다. 값이 없으면 명시적으로 비운다.
안 비우면 대상을 안 고른 액션이 앞 누름의 유닛으로 나간다.

**대상이 빈 액션에서 자가시전만 끄는 것**은 `implementing-focus-and-self-cast.md` §3-13이 정한
대로 원본을 `target`에 고정해서 이 길에 태운다. 겨눌 유닛을 내는 자리가 둘이라는 것에 주의한다.
`GetBindingInfoForAction`의 원본과, 프로브 바인딩을 채우는 자리가 `action.unit`을 각각 읽는다.
한쪽만 고치면 흑마 디스펠 같은 프로브 바인딩이 혼자 대상 빈 채로 남는다.

## 5. 못 얹는 자리

**매크로 타입 액션.** 바깥이 매크로가 되면 안쪽 매크로가 안 돈다(§2-2). 커스텀 매크로 액션에는
이 줄들을 못 붙이고 화면에서 잠겨야 한다. 우리 탈것 액션도 여기 들어간다.
`C_MountJournal.SummonByID`를 부르는 매크로라서, 자동 변신 해제가 제일 걸리는 자리가 바로 못
얹는 자리다. 거기는 드루이드에 `/cancelform`을 손으로 붙여 둔 상태가 이미 있다.

**유지·시전 주문은 지금은 빠지지만 못 넣을 이유는 없다.** 본문을 엣지마다 다르게 쓰면 나른다는
것이 2026-09-21에 게임에서 나왔다(§2-2의 인용, `DebindDev/Probe_PressHold.lua`). 이번 판에 같이
넣을지는 안 정했다. 넣으면 누를 때와 뗄 때 다른 본문을 쓰는 일이 이 트랙에 붙는다.

## 6. 손댈 자리

- `Misc.lua` 겨눌 유닛을 내는 헬퍼 하나로 모으고, 원본과 프로브 두 자리가 그것을 읽게 한다.
- `Misc.lua` 값 읽기. `CastingValue`를 쓰는 헬퍼로 네 줄을 답한다.
- `ActionMenuItems.lua` `CreateCastingMenu`에 네 줄. 잠금 이유 문구는 `CAST_KEY_TARGET_PICKED`가
  쓰는 방식을 따른다.
- `ActionTooltip.lua` 기본이 아닌 줄만 그린다.
- `Locales/*.lua` 문구. 클라이언트가 이미 가진 이름이 넷 중 셋에 있다
  (`AUTO_SELF_CAST_TEXT`, `AUTO_DISMOUNT_FLYING_TEXT`). 자동 변신 해제는 클라이언트 문자열이
  없어서 새로 짜야 한다.
- `DebindStorage/Export.lua`의 `CASTING_TYPES`에 이름 넷. 가져오기가 같은 표를 쓰므로 빠지면
  공유된 액션에서 값이 조용히 사라진다.
- `Profile.lua` 정리기에 기본값 지우는 줄.
- `SecureBindings.lua` 게이트와 본문 굽기.
- `Debind.lua` `CastFrame`의 `unit` 비우기.

**마이그레이션은 없다.** 값이 없는 것이 새 기본값이고, 모르는 값은 기본으로 읽힌다.

## 7. 커버리지

**헤드리스로 재는 것.** 액션의 값에서 바인딩이 어떻게 파생되는지, `target` 고정이 원본과 프로브
양쪽에 걸리는지, 정리기가 기본값을 지우는지, 내보내기와 가져오기가 네 이름을 나르는지.

**헤드리스가 못 보는 것.** 구운 본문이 실제로 CVar를 움직이는지, 그래서 시전이 어디로 가는지.
본문 문자열까지는 검사가 볼 수 있지만 그 본문이 게임에서 하는 일은 못 본다.

**원리적으로 못 재는 것.** 자동 변신 해제와 자동 탈것 내리기를 껐을 때 화면에 무엇이 뜨는지.
빨간 글씨가 나는지 아무 말도 없는지는 클라이언트가 정하고 이벤트로 안 나올 수 있다.

## 8. 아직 안 정한 것

- 화면 문구와 네 줄의 배치. 메뉴가 이미 길다.
- 잠금 규칙의 정확한 조건. 매크로 타입은 확실하고, 액션 종류마다 뜻이 없는 줄이 더 있는지는 안
  봤다.
- 유지·시전 주문을 이번 판에 같이 넣을지.
