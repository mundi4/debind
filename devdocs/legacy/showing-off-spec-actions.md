> 상태 (2026-08-24): **닫혔다.** 남아 있던 "안 정한 것"은 `devdocs/building-export-import.md`
> §7과 같은 물음이었고, 다른 직업/캐릭터 액션은 화면에서 알리지 않기로 하면서 거기서 같이
> 풀렸다(소유자 결정). 이 문서는 `devdocs/legacy/`로 옮긴다.

# 오버뷰가 오프스펙 액션까지 보여준다

## 무엇이 문제였나

왼쪽 열은 `EnumerateProfileLayers()`만 훑었다. 그건 "지금 이 특성에서 도는 것"을 답하는
함수라, **다른 전문화에 걸어둔 것은 이 화면 어디에도 안 나왔다** — 오른쪽 탭을 하나씩 열어
뒤지는 수밖에 없었다.

그 자리에 근거가 적혀 있긴 했다. *"이 열의 문장이 '지금 이 키보드'라서, 따라가면 그 문장이
화면마다 달라진다."* 그 문장은 이미 두 번 무너져 있었다 — 격리가 꺼진 액션을 목록에 세웠고
(키도 있고 목록에도 있는데 눌러도 아무 일이 없다), 오프스펙이 세 번째였다. **약속을 지킬 수
없게 됐으면 문장을 고치는 것이 맞지, 화면을 좁히는 것이 아니다.**

## 어디에 놓나 — "자기가 활성이었다면 섰을 자리"

레이어 비교가 **스코프까지만** 본다. 특성 번호는 안 본다.

```
캐릭터/특성 → 캐릭터/공용 → 직업/특성 → 직업/공용 → 일반
```

오프스펙 액션은 자기 스코프의 그 밴드에 그대로 들어간다. 그러니 **캐릭터/특성3의 오프스펙
행이 직업/특성2의 활성 행보다 위에 설 수 있다.** 자리는 "활성이었다면 어디였을까"가 정하고,
지금 안 돈다는 것은 자리가 아니라 사유 칸이 말한다.

## 비교 차례

```
중요도 → hover → 조건 → 레이어(스코프) → 전문화 → seq
```

**전문화가 `seq`보다 앞이다.** `seq`는 한 레이어 안에서만 믿을 수 있는 값이라, 레이어 하나로
좁혀지기 전에 비교하면 서로 다른 번호 공간을 견주게 된다. 레이어 단계가 스코프까지만 좁히므로
그 자리를 마저 좁히는 것이 이 단계다. 활성이 0이라 언제나 앞이고, 오프끼리는 특성 번호 차례다.

- 처음 적었던 안은 전문화를 `seq` **뒤**에 뒀다. 그러면 다른 레이어의 1번과 1번을 먼저
  견주게 되어 뜻이 없다. 소유자가 그 자리에서 바로잡았다.
- **활성끼리의 차례는 한 칸도 안 바뀐다.** 실제로 발동하는 액션은 전부 활성 레이어에 있어서
  이 단계에서 언제나 동률이다. `tests/overview_spec.lua`가 그걸 못 박는다 — 바뀌면 저장
  데이터는 그대로인데 전 사용자의 발동 순서가 조용히 달라지고, 공유 레이어 때문에 되돌리는
  마이그레이션을 쓸 수가 없다.

### 키 없이 도착한 그룹

> ⚠ **이 절은 2026-08-15에 갈아엎혔다** (`building-export-import.md`의 세 번째 ★). 여기 적힌
> `importOrder`는 **없어졌다** — 키 없이 도착하는 그룹 자체가 없어졌기 때문이다. 키를 빼고 온
> 세트는 **숫자 키**를 달고 오고, 그러면 키 그룹이라 `seq`가 제자리에서 차례를 말한다.
> **남겨두는 이유는 아래 마지막 문단 때문이다** — 레이어·전문화가 그룹 안 차례보다 앞이라는
> 것은 `seq`에도 그대로여서, 갈리는 경우와 그것이 맞는 이유가 지금도 유효하다.

~~그 액션들에는 `seq`가 없다(`PlaceLast`가 키 없으면 번호를 안 준다). 비교자가 `seq`를 볼
자리에서 `seq or importOrder`로 읽는다.~~ 안 그러면 그 그룹은 마지막 단계에서 통째로 동률이
되어 **보낸 사람의 순서가 사라진다** — 키가 빠진 문자열에서 그것이 설계의 유일한 잔존물이다.
그 물음은 그대로 남아 있고, 답이 `importOrder`에서 **숫자 키 + `seq`**로 바뀌었을 뿐이다.

~~**`seq`는 키가 있을 때만 읽는다**(`MakeRow`, 그리고 익스포트의 같은 자리).~~ 그 가드도 같이
없어졌다. 지키던 것이 "키 없이 사는 도착 그룹"이었는데 그런 것이 없다.

**레이어·전문화가 그룹 안 차례보다 앞이다.** 그러니 레이어를 넘는 도착 그룹은 보낸 사람의
차례가 아니라 **읽는 사람의 차례**로 그려질 수 있다. 보통은 둘이 같다 — 중요도·hover·조건·
레이어가 전부 실려 오기 때문이다. 갈리는 경우는 하나뿐이고, 갈리는 것이 맞다: 받는 쪽은 자기
활성 특성을 앞에 두므로, 한 그룹이 특성 레이어 둘에 걸쳐 있으면 읽는 사람의 활성 특성이 먼저 온다.

### 그룹에 안 속한 것

`NOT_BOUND` 더미는 **이름순**이다. 발동 순서는 발동하는 것들 사이에서만 뜻이 있는데 이것들은
키가 아예 없고 아무 키 그룹에도 안 속한다. 남는 기준은 읽는 사람이 눈으로 찾는 것, 즉 무엇이냐다.

이름이 같을 때의 tiebreak는 `layerRank`가 아니라 **`layerID`**다. `layerRank`가 스코프가 된
뒤로는 캐릭터 특성 넷이 같은 값이라, 서로 다른 특성의 첫 액션 둘이 `index`까지 같아져 완전히
동률이 된다 — `table.sort`는 안정 정렬이 아니므로 그리는 때마다 자리가 바뀐다.

## 오프스펙 행의 생김새

배지 행과 같은 처리다. **지금 이 특성의 키 맵에 없는 것은 순서에도 없다**는 같은 규칙이다.

- 사유 칸은 `ORDER_FLAG_OFFSPEC`("비활성 전문화"). 자리만으로는 활성과 구별이 안 되므로 이
  줄이 그걸 말하는 유일한 자리다. 어느 전문화인지는 행 툴팁이 레이어 이름으로 말한다.
  - **낱말은 클라이언트 것을 쓴다.** 전문화를 지금 것으로 만드는 버튼이
    `TALENT_SPEC_ACTIVATE`("활성화")라 상태는 활성/비활성이고, 반대쪽은 `FACTION_INACTIVE`
    ("비활성")에 이미 있다. 처음엔 "다른 전문화"라고 적었는데 우리가 지어낸 말인 데다 뜻도
    빗나갔다 — "어딘가 다른 전문화"로 읽히지만 요점은 **꺼져 있다**는 것이다.
  - 같은 이유로 주문 선택창의 `SPELL_PICKER_SHOW_OFFSPEC`도 같은 낱말로 맞췄다. 한 창에서
    같은 것을 두 이름으로 부르면 읽는 사람은 둘이라고 생각한다.
- ↑↓ 없음. 옆 행이 사유를 계산할 때 상대로도 안 친다.
- `unreachable`을 계산하지 않는다. 그 값은 **지금 이 특성으로 만들어진 키 맵**에서 나온 것이라
  그 세계에서는 참이 아니다 — `simulated`에 이미 같은 이유가 적혀 있고, 오프스펙 행을 같은
  경우로 접었다.
- 우클릭 메뉴도 같은 답을 낸다. `ComputeOrderSwap`이 오프스펙을 건너뛰므로 대상이 오프스펙이면
  이웃이 활성으로 나오고, 전문화 단계에서 갈려 `ORDER_BLOCKED_SPEC`이 뜬다.

## 내보내기도 같은 비교자로

`order`를 `CompareActionOrder`가 매긴다. 이 애드온에서 순서를 말하는 자리가 하나뿐이어야
문자열과 화면이 갈리지 않는다.

**활성 특성은 없다고 놓고 부른다**(`EnumerateAllProfileLayers(0)`). 활성이 있으면 그 레이어가
앞으로 당겨져서, **같은 프로필인데 보내는 사람이 그때 무슨 전문화였느냐로 문자열이 달라진다.**
없다고 놓으면 모든 특성 레이어가 자기 번호로 줄을 서고, 나오는 것은 보낸 사람의 프로필이지
보낸 사람의 오후가 아니다. 받는 쪽에서는 어차피 **읽는 사람의 활성 전문화 기준으로** 다시
정렬된다.

## 문자열도 같이 훑었다

동작이 바뀌면 그걸 설명하는 문장이 조용히 거짓이 된다. 검사가 못 잡는 종류라 같은 편집에서
훑었다.

- `OVERVIEW_DESC` — "여기 있는 것은 지금 누르면 실제로 일어날 일"을 뺐다. 대신 **자리가 무엇을
  뜻하는지**를 말한다. 첫 문장도 고쳤다 — "키가 지정된 모든 행동"으로 열어놓고 셋째 문단에서
  키 없는 것도 나온다고 말하고 있었다. 이 열은 **이 캐릭터의 지정 전부**다.
- `OVERVIEW_EMPTY_IMPORTED_ONLY` — "오른쪽의 다른 탭을 봐 주세요"가 거짓이 됐다. 오프스펙이
  이제 여기 나오므로 가리킬 탭이 없다. **남은 경우는 다른 직업의 레이어뿐이고**, 거기엔 탭
  자체가 없다 — 그 직업으로 접속해야 보인다.

## [결정] 다른 직업 레이어에 남은 배지는 화면에서 말하지 않는다 (2026-08-24, 소유자)

위 빈 문장이 임시로 메우고 있던 자리였다. `CollectImportedActions`가 `LayerArray`만 보는 것은
그대로 맞고(판단할 수 없는 것을 [N개 모두 받기]가 받으면 안 된다), 그래서 스트립이 사라진
뒤에도 계정 어딘가에 배지가 남을 수 있다. **그 자리를 화면에서 알리지 않기로 했다.** 다른
직업/캐릭터 액션을 이 캐릭터의 어떤 화면에도 안 띄운다는 원칙이 여기서도 그대로 이어진다. 남은
배지는 그 직업으로 접속해야 보인다. `devdocs/building-export-import.md` §7과 같은 물음이었고,
거기서 같이 닫혔다.

**그룹째 키 주기도 같은 답으로 정리된다.** `CollectImportGroupActions` /
`CollectKeyGroupActions`가 이 캐릭터의 열한 레이어까지만 모으고, 한 묶음이 반만 처리되고
나머지는 그 직업으로 접속하는 날 자기 키를 받는 지금 동작이 최종 답이다 — 읽는 사람이 그
키보드를 볼 수 있게 하지 않기로 했으므로 다시 볼 이유가 없다.

## 딸려서 고친 것

- **`DebindTest`의 격리 이음매**(`SetIsolated`). `EnumerateProfileLayers` 하나만 바꿔치기하고
  있었는데 오버뷰와 순서가 이제 `EnumerateAllProfileLayers`를 읽는다. 그대로 뒀으면 격리 런
  도중에 **그 경로들이 테스터의 진짜 프로필을 보고** `BuildKeyMap`만 테스트 레이어를 드는
  반쪽짜리가 된다. 값 둘(스코프, 특성)을 같이 내야 하는 것도 여기서 처음 생긴 요구다.
- **`PlanImport`이 주소를 먼저 묻고 필터를 나중에 본다.** 줄은 놓일 수 있는 것으로만 세우므로,
  놓을 데가 없는 액션은 애초에 물어본 적이 없어서 "안 골랐다"로 읽히면 안 된다. 필터를 먼저 보던
  코드는 그것들을 조용히 버렸다 — 창은 "2개를 가져왔습니다"라고 말하고 못 넣은 다섯은 입에
  올리지 않았다. 같은 편집에서 `IMPORT_COMMITTED_SKIPPED`를 그 경우까지 말하도록 고쳐놓고 정작
  그 경우가 안 세어지고 있었다.
